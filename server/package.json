{
  "name": "fickle-bean-reader",
  "version": "0.0.0",
  "scripts": {
    "build": "npx tsc",
    "start": "ts-node -r dotenv/config ./dist/server --env=production dotenv_config_path=./env/production.env",
    "dev": "ts-node -r dotenv/config ./dist/server --env=development dotenv_config_path=./env/development.env",
    "test": ""
  },
  "nodemonConfig": {
    "watch": [
      "src"
    ],
    "ext": "ts, html",
    "ignore": [
      "src/public"
    ],
    "exec": "./node_modules/.bin/ts-node --files -r tsconfig-paths/register ./src"
  },
  "_moduleAliases": {
    "@src": "dist"
  },
  "engines": {
    "node": ">=8.10.0"
  },
  "dependencies": {
    "dotenv": "^16.4.4",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "module-alias": "^2.2.3",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.17",
    "typescript": "^5.3.3"
  }
}
